# Rust / Cargo
# 各パッケージのビルド成果物（ルート直下・サブディレクトリ問わず無視）
/target/
**/target/

# Cargo.lock は実行バイナリ（アプリ）ではコミット推奨のため無視しない
# ライブラリとして公開する場合は次の行のコメントを外す
# Cargo.lock

# バックアップ・一時ファイル
**/*.rs.bk
*.pdb

# macOS
.DS_Store
